## What does this PR do?

A short description of the change and why it's needed.

## Related issue

Closes #

## Checklist

- [ ] `./gradlew test` passes locally.
- [ ] `./gradlew assembleDebug` builds successfully.
- [ ] New/changed `domain/` behavior has unit test coverage.
- [ ] No new required network access, analytics, ads, or telemetry was added.
- [ ] No code or docs imply live Chrome-Android tab access exists (see
      `docs/PLATFORM_LIMITATIONS.md`) unless this PR is specifically about that constraint.
- [ ] Docs updated if this changes architecture, export formats, or supported platform
      capabilities.

## Screenshots (if UI change)

Before / after, if applicable.
