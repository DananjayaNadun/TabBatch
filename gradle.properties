org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
android.suppressUnsupportedCompileSdk=35
# The checkout path on this machine contains a non-ASCII em dash character
# (Windows folder name), which trips AGP's path-safety warning-as-error.
# This has no effect on correctness; it's purely a Windows path-encoding check.
android.overridePathCheck=true
