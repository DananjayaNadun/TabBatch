# Add project specific ProGuard rules here.
# TabBatch ships with minification disabled by default (see app/build.gradle.kts).
# These rules are kept for when a maintainer enables R8/minification for release builds.
